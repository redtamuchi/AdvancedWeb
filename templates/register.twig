<!doctype html>


<html>
    {% include 'partials/head.twig' %}
    <body>
    
    {% include 'partials/navigation.twig' %}
        <main clss="container-fuid">
         <div class="row">
          <div class="col-md-6 offset-md-3">
           <form id"register-form" action="register.php" method="post">
            <h1>Register for an account</h1>
             <div class="form-group">
              <lable for="email">Email</lable>  
                 <input type="email" id="email" name="email" placeholder="you@example.com" class="form-control">
                 <span class="text-danger">{% if register.errors.email %}</span>
                    <span class="invaild-feedback">{{register.errors.password}}</span>  
                    {% endif %}               
                 </div>
                <div class="form-group">
                 <lable for="password">Password</lable>
                 <input type="password" id="password" name="password" placeholder"minimum 8 characters" class="form-control">
                  {% if register.success %}
                    <div class="alert alert-success">
                        Your account has been created!
                        </div>
                        {% endif %}
                  </div>
                  <button class="btn btn-primary" type="submit">Register</button>
                    </form>
                    </div>
                </div>
            </div>
        </main>
    </body>
</html>