<div class="navbar sticky-top navbar-dark bg-dark navbar-expand-lg">
    <a href="index.php" class="navbar-brand">Shop</a>
    <button class="navbar-toggler" data-target="#main-nav" data-toggle="collapse">
        <span class="navbar-toggler-icon"></span>
    </button>
    <form class="form-inline" method="get" action="search.php">
        <input type="search" name="query" placeholder="Search" class="form-control">
        <button type="sumbmit" class"btn btn-primary">Search</button>
    
    
    </form>
    <div class="collapse navbar-collapse" id="main-nav">
        <nav class="nav navbar-nav">
        {% for item in navigation.navigation %}
            {% set active = ( item.url == navigation.active ) ? 'active' : '' %}
            <a class="nav-link" href="{{item.url}}">{{item.menu}}</a>
            {% endfor %}
            
        </nav>
    </div>
</div>